language: python

notifications:
  email: false

jobs:
  include:
    - os: osx
      osx_image: xcode12.2
      language: generic
      env: TOXENV=py37

before_install:
  - chmod u+x .travis/before_install.sh
  - .travis/before_install.sh

install:
  - chmod u+x .travis/install.sh
  - .travis/install.sh

script:
  - chmod u+x .travis/script.sh
  - .travis/script.sh