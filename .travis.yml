notifications:
  email: false

matrix:
  include:
    - sudo: required
      services:
        - docker
      env: DOCKER_IMAGE=quay.io/pypa/manylinux2014_x86_64
           PLAT=manylinux2014_x86_64

install:
  - docker build -t blenderpy/manylinux -f .docker/manylinux/Dockerfile .

script:
  - docker run blenderpy/manylinux