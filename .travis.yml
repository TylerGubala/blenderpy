language: python

notifications:
  email: false

jobs:
  include:
    - os: osx
      language: generic
      env: TOXENV=py37

before_install:
  - .travis/before_install.sh

install:
  - .travis/install.sh

script:
  - .travis/script.sh